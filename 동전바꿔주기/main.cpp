#include <stdio.h>
int main ()
{
    int t,k,p[100],n[100];
    scanf("%d",&t);
    scanf("%d",&k);
    for(int i=0; i<k; i++){
        scanf("%d %d",&p[i],&n[i]);
    }
    for(int r=0; r<k; r++){

    }

    return 0;
}
